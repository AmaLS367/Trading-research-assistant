# 🔧 Руководство по устранению неполадок

**Распространенные проблемы и решения**

[![Support](https://img.shields.io/badge/Support-Troubleshooting-FF6B6B)](./troubleshooting.md)
[![Status](https://img.shields.io/badge/Status-Active-success)](./troubleshooting.md)

---

## 📋 Содержание

<details>
<summary>Нажмите, чтобы развернуть</summary>

- [Проблемы установки](#-проблемы-установки)
- [Проблемы конфигурации](#-проблемы-конфигурации)
- [Ошибки подключения к API](#-ошибки-подключения-к-api)
- [Проблемы с LLM/Ollama](#-проблемы-с-llmollama)
- [Проблемы с базой данных](#-проблемы-с-базой-данных)
- [Проблемы получения данных](#-проблемы-получения-данных)
- [Проблемы производительности](#-проблемы-производительности)
- [Общие советы](#-общие-советы)

</details>

---

## 🚀 Проблемы установки

### Проблема: Команда `uv` не найдена

**Симптомы:**
```bash
$ uv sync
bash: uv: command not found
```

**Решения:**

<details>
<summary><strong>Windows</strong></summary>

1. Установите через PowerShell:
   ```powershell
   powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
   ```

2. Перезапустите терминал или добавьте в PATH вручную

3. Проверьте установку:
   ```bash
   uv --version
   ```

</details>

<details>
<summary><strong>Linux/macOS</strong></summary>

1. Установите через curl:
   ```bash
   curl -LsSf https://astral.sh/uv/install.sh | sh
   ```

2. Добавьте в PATH (если нужно):
   ```bash
   export PATH="$HOME/.cargo/bin:$PATH"
   ```

3. Проверьте установку:
   ```bash
   uv --version
   ```

</details>

---

### Проблема: Несовместимость версии Python

**Симптомы:**
```
Error: requires-python = ">=3.11"
```

**Решения:**

1. **Проверьте версию Python:**
   ```bash
   python --version
   ```

2. **Установите Python 3.11+** при необходимости:
   - Windows: Скачайте с [python.org](https://www.python.org/downloads/)
   - Linux: `sudo apt install python3.11` (Ubuntu/Debian)
   - macOS: `brew install python@3.11`

3. **Используйте конкретную версию Python с uv:**
   ```bash
   uv python install 3.11
   uv sync --python 3.11
   ```

---

## ⚙️ Проблемы конфигурации

### Ошибка: "No market data provider configured"

**Симптомы:**
```
RuntimeError: No market data provider configured
```

**Причина:** API ключи для провайдеров данных не настроены.

**Решения:**

<details>
<summary><strong>Пошаговое исправление</strong></summary>

1. **Проверьте наличие файла `.env`:**
   ```bash
   ls -la .env  # Linux/macOS
   dir .env     # Windows
   ```

2. **Скопируйте `.env.example` в `.env`** если отсутствует:
   ```bash
   cp .env.example .env  # Linux/macOS
   Copy-Item .env.example .env  # Windows (PowerShell)
   ```

3. **Добавьте необходимые API ключи:**
   ```bash
   # OANDA (основной)
   OANDA_API_KEY=your_oanda_api_key_here
   OANDA_BASE_URL=https://api-fxpractice.oanda.com
   
   # ИЛИ Twelve Data (резервный)
   TWELVE_DATA_API_KEY=your_twelve_data_key
   TWELVE_DATA_BASE_URL=https://api.twelvedata.com
   ```

4. **Проверьте конфигурацию:**
   ```bash
   python -c "from src.app.settings import settings; print(settings.oanda_api_key[:10] if settings.oanda_api_key else 'Not set')"
   ```

</details>

**Профилактика:**
- Всегда используйте файл `.env` (никогда не коммитьте его в git)
- Тестируйте API ключи перед запуском анализа
- Используйте резервные провайдеры для надежности

---

### Ошибка: "OLLAMA_MODEL must be set"

**Симптомы:**
```
ValueError: OLLAMA_MODEL must be set
```

**Причина:** Модель Ollama не указана в переменных окружения.

**Решения:**

1. **Установите модель в `.env`:**
   ```bash
   OLLAMA_MODEL=llama3.2
   # или
   OLLAMA_MODEL=llama3.1
   # или любая другая модель, которая у вас есть
   ```

2. **Список доступных моделей:**
   ```bash
   ollama list
   ```

3. **Загрузите модель, если отсутствует:**
   ```bash
   ollama pull llama3.2
   ```

4. **Проверьте доступность модели:**
   ```bash
   ollama run llama3.2 "Привет"
   ```

---

## 🌐 Ошибки подключения к API

### Проблема: Таймаут подключения к OANDA API

**Симптомы:**
```
httpx.ConnectTimeout: Connection timeout
```

**Решения:**

<details>
<summary><strong>Диагностические шаги</strong></summary>

1. **Проверьте подключение к API:**
   ```bash
   curl -H "Authorization: Bearer YOUR_API_KEY" https://api-fxpractice.oanda.com/v3/accounts
   ```

2. **Проверьте настройки firewall/proxy:**
   - Убедитесь, что порт 443 (HTTPS) открыт
   - Настройте proxy, если за корпоративным firewall

3. **Проверьте права API ключа:**
   - Проверьте настройки аккаунта OANDA
   - Убедитесь, что API ключ имеет права на чтение

4. **Попробуйте резервный провайдер:**
   - Установите `TWELVE_DATA_API_KEY` в `.env`
   - Система автоматически переключится

</details>

**Распространенные причины:**
- Проблемы с сетевым подключением
- Неверный URL конечной точки API
- Истекший или недействительный API ключ
- Ограничение скорости (слишком много запросов)

---

### Проблема: Ограничение скорости GDELT API

**Симптомы:**
```
HTTP 429: Too Many Requests
```

**Решения:**

1. **Добавьте задержки между запросами:**
   ```python
   # Уже обработано в коде, но можно настроить:
   RUNTIME_NEWS_REFRESH_INTERVAL_SECONDS=600  # Увеличьте до 10 минут
   ```

2. **Используйте NewsAPI как резервный:**
   ```bash
   NEWSAPI_API_KEY=your_newsapi_key
   ```

3. **Уменьшите частоту запросов новостей:**
   - Запрашивайте новости только при необходимости
   - Кэшируйте результаты новостей

---

## 🤖 Проблемы с LLM/Ollama

### Проблема: Отказ в подключении к Ollama

**Симптомы:**
```
ConnectionRefusedError: [Errno 111] Connection refused
```

**Решения:**

<details>
<summary><strong>Локальный Ollama</strong></summary>

1. **Запустите сервис Ollama:**
   ```bash
   ollama serve
   ```

2. **Проверьте, что сервис запущен:**
   ```bash
   curl http://localhost:11434/api/tags
   ```

3. **Проверьте конфигурацию `.env`:**
   ```bash
   OLLAMA_BASE_URL=http://localhost:11434
   ```

</details>

<details>
<summary><strong>Удаленный Ollama</strong></summary>

1. **Проверьте сетевое подключение:**
   ```bash
   ping your-ollama-server.com
   curl http://your-ollama-server.com:11434/api/tags
   ```

2. **Проверьте правила firewall:**
   - Убедитесь, что порт 11434 открыт
   - Проверьте настройки группы безопасности (AWS/GCP)

3. **Обновите `.env`:**
   ```bash
   OLLAMA_BASE_URL=http://your-ollama-server.com:11434
   ```

4. **Проверьте подключение:**
   ```bash
   python -c "import httpx; print(httpx.get('http://your-ollama-server.com:11434/api/tags').json())"
   ```

</details>

---

### Проблема: Модель LLM не найдена

**Симптомы:**
```
Error: model 'llama3.2' not found
```

**Решения:**

1. **Список доступных моделей:**
   ```bash
   ollama list
   ```

2. **Загрузите модель:**
   ```bash
   ollama pull llama3.2
   ```

3. **Проверьте имя модели в `.env`:**
   ```bash
   OLLAMA_MODEL=llama3.2  # Должно точно совпадать
   ```

4. **Проверьте модель:**
   ```bash
   ollama run llama3.2 "тест"
   ```

---

### Проблема: Медленные ответы LLM

**Симптомы:**
- Анализ занимает слишком много времени
- Ошибки таймаута

**Решения:**

1. **Используйте меньшую/быструю модель:**
   ```bash
   OLLAMA_MODEL=llama3.2:1b  # Меньшая квантованная версия
   ```

2. **Увеличьте настройки таймаута:**
   ```python
   # В коде, настройте таймаут LLM при необходимости
   ```

3. **Используйте ускорение GPU:**
   ```bash
   # Убедитесь, что CUDA/ROCm доступен
   ollama run llama3.2 --gpu
   ```

4. **Уменьшите окно контекста:**
   - Используйте более короткие промпты
   - Ограничьте исторические данные

---

## 💾 Проблемы с базой данных

### Проблема: База данных заблокирована

**Симптомы:**
```
sqlite3.OperationalError: database is locked
```

**Решения:**

<details>
<summary><strong>Быстрые исправления</strong></summary>

1. **Закройте другие экземпляры:**
   - Проверьте наличие нескольких процессов Python
   - Закройте просмотрщики базы данных (DB Browser и т.д.)

2. **Проверьте файлы блокировки:**
   ```bash
   ls -la db/*.sqlite3-*  # Linux/macOS
   dir db\*.sqlite3-*     # Windows
   ```

3. **Удалите файлы блокировки** (если безопасно):
   ```bash
   rm db/*.sqlite3-*  # Linux/macOS
   del db\*.sqlite3-*  # Windows
   ```

</details>

**Профилактика:**
- Используйте пул соединений
- Правильно закрывайте соединения с базой данных
- Избегайте параллельных записей

---

### Проблема: Ошибки миграции

**Симптомы:**
```
sqlite3.OperationalError: table already exists
```

**Решения:**

1. **Проверьте статус миграции:**
   ```bash
   sqlite3 db/forex_research_assistant.sqlite3 ".schema"
   ```

2. **Сбросьте базу данных** (⚠️ **ВНИМАНИЕ**: Удаляет все данные):
   ```bash
   rm db/forex_research_assistant.sqlite3  # Linux/macOS
   del db\forex_research_assistant.sqlite3  # Windows
   python src/app/main.py init-db
   ```

3. **Ручная миграция:**
   ```bash
   sqlite3 db/forex_research_assistant.sqlite3 < src/storage/sqlite/migrations/0001_init.sql
   ```

---

## 📊 Проблемы получения данных

### Ошибка: "Insufficient candles: got X, need at least 200"

**Симптомы:**
```
ValueError: Insufficient candles: got 150, need at least 200
```

**Причина:** Недостаточно исторических данных от провайдера.

**Решения:**

<details>
<summary><strong>Диагностический чеклист</strong></summary>

1. **Проверьте формат символа:**
   ```bash
   # Правильно: EURUSD, GBPUSD, USDJPY
   # Неправильно: EUR/USD, EUR-USD
   ```

2. **Проверьте доступность данных:**
   - OANDA: Убедитесь, что символ торгуется
   - Twelve Data: Проверьте символ в их базе данных

3. **Попробуйте другой таймфрейм:**
   ```bash
   # Если 1h не работает, попробуйте:
   python src/app/main.py analyze --symbol EURUSD --timeframe 1d
   ```

4. **Увеличьте размер окна** (при необходимости):
   ```bash
   RUNTIME_MARKET_DATA_WINDOW_CANDLES=150  # Уменьшите требование
   ```

5. **Проверьте статус провайдера:**
   - Страница статуса OANDA
   - Статус Twelve Data

</details>

---

### Проблема: Данные новостей недоступны

**Симптомы:**
```
No news found for symbol EURUSD
```

**Решения:**

1. **Проверьте доступность GDELT:**
   ```bash
   curl "https://api.gdeltproject.org/api/v2/doc/doc?query=forex%20EURUSD&mode=artlist&format=json"
   ```

2. **Используйте резервный NewsAPI:**
   ```bash
   NEWSAPI_API_KEY=your_key
   ```

3. **Настройте параметры запроса новостей:**
   - Попробуйте более широкие поисковые термины
   - Проверьте диапазоны дат

---

## ⚡ Проблемы производительности

### Проблема: Анализ занимает слишком много времени

**Симптомы:**
- Анализ выполняется > 5 минут
- Система перестает отвечать

**Решения:**

1. **Уменьшите окно данных:**
   ```bash
   RUNTIME_MARKET_DATA_WINDOW_CANDLES=200  # Вместо 300
   ```

2. **Отключите подробный вывод:**
   ```bash
   # Удалите флаг --verbose
   python src/app/main.py analyze --symbol EURUSD --timeframe 1h
   ```

3. **Оптимизируйте вызовы LLM:**
   - Используйте меньшую модель
   - Уменьшите размер промпта
   - Кэшируйте ответы

4. **Проверьте ресурсы системы:**
   ```bash
   # Мониторинг CPU/Память
   top  # Linux/macOS
   taskmgr  # Windows
   ```

---

### Проблема: Высокое использование памяти

**Симптомы:**
- Система исчерпывает память
- Ошибки OOM (Out of Memory)

**Решения:**

1. **Уменьшите pandas dataframes:**
   - Обрабатывайте данные частями
   - Очищайте неиспользуемые переменные

2. **Ограничьте параллельные операции:**
   - Запускайте один анализ за раз
   - Правильно закрывайте соединения

3. **Используйте потоковую обработку данных:**
   - Не загружайте все данные сразу
   - Обрабатывайте инкрементально

---

## 💡 Общие советы

### Лучшие практики

<details>
<summary><strong>Конфигурация</strong></summary>

- ✅ Всегда используйте файл `.env` для секретов
- ✅ Тестируйте API ключи перед использованием в продакшене
- ✅ Используйте резервные провайдеры для надежности
- ✅ Мониторьте ограничения скорости API
- ✅ Поддерживайте зависимости в актуальном состоянии

</details>

<details>
<summary><strong>Разработка</strong></summary>

- ✅ Используйте виртуальные окружения
- ✅ Запускайте тесты перед развертыванием
- ✅ Регулярно проверяйте логи
- ✅ Мониторьте ресурсы системы
- ✅ Делайте резервные копии базы данных

</details>

<details>
<summary><strong>Продакшен</strong></summary>

- ✅ Используйте менеджеры процессов (systemd, supervisor)
- ✅ Настройте логирование и мониторинг
- ✅ Реализуйте проверки здоровья
- ✅ Используйте пул соединений
- ✅ Планируйте регулярные резервные копии

</details>

---

### Команды для отладки

```bash
# Проверка конфигурации
python -c "from src.app.settings import settings; print(settings.model_dump_json(indent=2))"

# Тест подключения к базе данных
python -c "from src.storage.sqlite.connection import DBConnection; db = DBConnection('db/forex_research_assistant.sqlite3'); print('OK')"

# Тест подключения к API
python -c "import httpx; print(httpx.get('https://api-fxpractice.oanda.com/v3/accounts', headers={'Authorization': 'Bearer YOUR_KEY'}).status_code)"

# Проверка Ollama
curl http://localhost:11434/api/tags
```

---

## 🆘 Получение помощи

Если проблемы все еще возникают:

1. **Проверьте логи:**
   - Логи приложения в `artifacts/`
   - Системные логи (journalctl, Просмотр событий)

2. **Изучите документацию:**
   - [Руководство по использованию](./usage_guide.md)
   - [Архитектура](./architecture.md)
   - [Политика безопасности](./safety_policy.md)

3. **Создайте отчет об ошибке:**
   - Включите сообщения об ошибках
   - Информацию о системе
   - Конфигурацию (без секретов)
   - Шаги для воспроизведения

---

[📖 Обзор](./overview.md) | [📚 Руководство по использованию](./usage_guide.md) | [🏗️ Архитектура](./architecture.md)
