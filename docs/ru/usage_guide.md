<div align="center">

# 📚 Руководство по использованию

**Инструкции по установке и использованию**

[![Python](https://img.shields.io/badge/Python-3.11+-3776AB?logo=python&logoColor=white)](https://www.python.org/)
[![uv](https://img.shields.io/badge/uv-Latest-FFD43B?logo=python&logoColor=black)](https://github.com/astral-sh/uv)

</div>

---

## Установка

### Требования

- Python 3.11 или выше
- uv (рекомендуется) или pip
- Ollama (для LLM функций)
- API ключи для провайдеров данных

### Установка зависимостей

#### С использованием uv (рекомендуется)

```bash
# Базовые зависимости (обязательно)
uv sync

# С зависимостями для разработки (тестирование, линтинг)
uv sync --extra dev
```

#### С использованием pip

```bash
# Базовые зависимости
pip install -e .

# С зависимостями для разработки
pip install -e ".[dev]"
```

## Настройка

### Переменные окружения

Создайте файл `.env` в корне проекта:

```bash
# --- Приложение ---
APP_ENV=development
APP_TIMEZONE=Asia/Yerevan

# --- OANDA API (основной провайдер) ---
OANDA_API_KEY=your_oanda_api_key_here
OANDA_ACCOUNT_ID=your_account_id
OANDA_BASE_URL=https://api-fxpractice.oanda.com

# --- Twelve Data API (резервный провайдер) ---
TWELVE_DATA_API_KEY=your_twelve_data_key
TWELVE_DATA_BASE_URL=https://api.twelvedata.com

# --- GDELT API (новости) ---
GDELT_BASE_URL=https://api.gdeltproject.org

# --- NewsAPI (опционально) ---
NEWSAPI_API_KEY=your_newsapi_key
NEWSAPI_BASE_URL=https://newsapi.org

# --- Ollama (LLM) ---
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# --- Хранилище ---
STORAGE_SQLITE_DB_PATH=db/forex_research_assistant.sqlite3
STORAGE_ARTIFACTS_DIR=artifacts
STORAGE_MIGRATION_PATH=src/storage/sqlite/migrations/0001_init.sql

# --- Runtime настройки ---
RUNTIME_MVP_SYMBOLS_RAW=EURUSD,GBPUSD,USDJPY
RUNTIME_MVP_TIMEFRAME=1h
RUNTIME_MVP_EXPIRY_SECONDS=300
RUNTIME_LLM_ENABLED=true
RUNTIME_LLM_CALL_INTERVAL_SECONDS=300
RUNTIME_NEWS_REFRESH_INTERVAL_SECONDS=300
RUNTIME_MARKET_DATA_WINDOW_CANDLES=300
```

### Инициализация базы данных

Перед первым использованием необходимо инициализировать базу данных:

```bash
python src/app/main.py init-db
```

Эта команда создаст SQLite базу данных и применит миграции.

## Основные команды

<details>
<summary><strong>📋 Быстрая справка по командам</strong></summary>

| Команда | Описание | Пример |
|---------|----------|--------|
| `init-db` | Инициализация базы данных | `python src/app/main.py init-db` |
| `analyze` | Запуск анализа | `python src/app/main.py analyze --symbol EURUSD --timeframe 1h` |
| `show-latest` | Просмотр последней рекомендации | `python src/app/main.py show-latest` |
| `show-latest --details` | Просмотр с полными обоснованиями | `python src/app/main.py show-latest --details` |
| `journal` | Логирование решения | `python src/app/main.py journal` |
| `report` | Просмотр статистики | `python src/app/main.py report` |

</details>

---

### Анализ символа

Запуск полного анализа символа:

```bash
python src/app/main.py analyze --symbol EURUSD --timeframe 1h
```

**Параметры:**
- `--symbol` (обязательно) — торговый символ (например, EURUSD, GBPUSD)
- `--timeframe` (опционально) — таймфрейм (1m, 5m, 15m, 30m, 1h, 4h, 1d). По умолчанию: 1h
- `--verbose` (опционально) — показать подробный вывод анализа во время выполнения (технический анализ, новости, синтез)

**Примеры:**

```bash
# Анализ EURUSD на часовом таймфрейме
python src/app/main.py analyze --symbol EURUSD --timeframe 1h

# Анализ GBPUSD на 15-минутном таймфрейме
python src/app/main.py analyze --symbol GBPUSD --timeframe 15m

# Анализ USDJPY на дневном таймфрейме
python src/app/main.py analyze --symbol USDJPY --timeframe 1d

# Анализ с подробным выводом
python src/app/main.py analyze --symbol EURUSD --timeframe 1h --verbose
```

**Проверка артефактов:**

После запуска анализа артефакты сохраняются в `artifacts/run_{run_id}/`:
- `recommendation.json` — JSON с данными рекомендации
- `rationales.md` — Markdown файл со всеми обоснованиями (Technical, News, Synthesis)

Для просмотра артефактов:
```bash
# Список всех директорий запусков
ls artifacts/

# Просмотр рекомендации для конкретного запуска
cat artifacts/run_123/recommendation.json

# Просмотр обоснований для конкретного запуска
cat artifacts/run_123/rationales.md
```

**Режим --verbose:**
При использовании флага `--verbose` система выводит детальную информацию на каждом этапе:
- **Technical Rationale** — полный текст технического анализа от LLM
- **News Digest** — агрегированный контекст новостей
- **Synthesis Logic** — логика синтеза рекомендации с действием, уверенностью и кратким обоснованием

Если текст слишком длинный (более 2000 символов), он будет обрезан с подсказкой использовать `show-latest --details` для просмотра полного текста.

**Что происходит:**

<details>
<summary><strong>📊 Детальный пайплайн анализа</strong></summary>

1. **Получение рыночных данных** 📈
   - Загружает исторические свечи из OANDA или Twelve Data
   - Проверяет качество и полноту данных
   - Применяет логику резервирования при сбое основного провайдера

2. **Расчет технических индикаторов** 🔢
   - Вычисляет RSI, MACD, Bollinger Bands и т.д.
   - Рассчитывает метрики волатильности
   - Определяет режим рынка (тренд/флэт)

3. **Технический анализ через LLM** 🤖
   - Отправляет снимок признаков в LLM
   - Получает техническое обоснование
   - Анализирует паттерны графиков и индикаторы

4. **Получение новостного контекста** 📰
   - Загружает релевантные новости из GDELT/NewsAPI
   - Агрегирует и фильтрует новостные статьи
   - Генерирует дайджест новостей с тональностью

5. **Синтез финальной рекомендации** 🎯
   - Объединяет технический и фундаментальный анализ
   - Генерирует действие (CALL/PUT/HOLD)
   - Назначает уровень уверенности (0.0-1.0)

6. **Сохранение в базу данных** 💾
   - Сохраняет рекомендацию
   - Сохраняет все обоснования
   - Связывает с метаданными запуска анализа

</details>

### Просмотр последней рекомендации

```bash
python src/app/main.py show-latest
```

Или с детальной информацией:

```bash
python src/app/main.py show-latest --details
```

**Без флага --details:**
Отображает последнюю сохраненную рекомендацию с цветовой индикацией:
- 🟢 Зеленый — CALL (покупка)
- 🔴 Красный — PUT (продажа)
- 🟡 Желтый — другие действия

Уверенность также отображается цветом:
- 🟢 Зеленая — высокая уверенность (≥70%)
- 🟡 Желтая — средняя уверенность (50-70%)
- 🔴 Красная — низкая уверенность (<50%)

**С флагом --details:**
После таблицы с рекомендацией выводятся сохраненные обоснования (rationales) из базы данных:
- **Technical Analysis** — технический анализ, выполненный LLM
- **News Context** — контекст новостей, использованный при анализе
- **AI Synthesis** — финальное обоснование рекомендации

Если у рекомендации нет `run_id` (старые записи, созданные до внедрения системы отслеживания запусков), выводится соответствующее сообщение.

### Журнал сделок

Интерактивная команда для логирования торговых решений:

```bash
python src/app/main.py journal
```

Команда:
1. Показывает последнюю рекомендацию
2. Спрашивает, была ли сделка открыта
3. Если нет — записывает причину пропуска
4. Если да — записывает результат (WIN/LOSS/DRAW) и комментарий

**Пример сессии:**

```
Latest Recommendation:
  Symbol: EURUSD
  Action: CALL
  Timeframe: 1h
  Timestamp: 2024-01-15 10:30:00

Did you take this trade? [y/N]: y
What was the result? [WIN/LOSS/DRAW]: WIN
How did you feel about the trade? [Confident/Nervous/Lucky]: Confident
Trade logged. Outcome ID: 42
```

### Отчет по статистике

Просмотр статистики торговли:

```bash
python src/app/main.py report
```

Отображает таблицу со статистикой по каждому символу:
- Общее количество сделок
- Процент выигрышей (WinRate)
- Количество выигрышей, проигрышей, ничьих
- Количество пропущенных сделок

## Рабочий процесс

<details>
<summary><strong>🎬 Интерактивная диаграмма рабочего процесса</strong></summary>

```
┌─────────────────┐
│  Начало анализа │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Загрузка       │
│  рыночных данных│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Расчет          │
│  индикаторов    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Анализ LLM     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Загрузка       │
│  новостей       │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Синтез         │
│  рекомендации   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Сохранение в БД│
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│ Просмотр и      │
│  решение        │
└─────────────────┘
```

</details>

---

### Типичный сценарий использования

1. **Запуск анализа:**
   ```bash
   python src/app/main.py analyze --symbol EURUSD --timeframe 1h
   ```

2. **Просмотр рекомендации:**
   ```bash
   python src/app/main.py show-latest
   ```

3. **Принятие решения:**
   - Изучите рекомендацию и обоснование
   - Примите решение о входе в сделку
   - Выполните сделку на брокере (вручную)

4. **Логирование результата:**
   ```bash
   python src/app/main.py journal
   ```

5. **Анализ статистики:**
   ```bash
   python src/app/main.py report
   ```

### Автоматизация

<details>
<summary><strong>⏰ Варианты автоматического планирования</strong></summary>

#### Linux/macOS (cron)

```bash
# Редактировать crontab
crontab -e

# Запуск каждый час
0 * * * * cd /path/to/project && /usr/bin/python3 src/app/main.py analyze --symbol EURUSD --timeframe 1h >> /var/log/trading_assistant.log 2>&1

# Запуск каждые 4 часа
0 */4 * * * cd /path/to/project && /usr/bin/python3 src/app/main.py analyze --symbol GBPUSD --timeframe 1h

# Запуск ежедневно в 9:00
0 9 * * * cd /path/to/project && /usr/bin/python3 src/app/main.py analyze --symbol USDJPY --timeframe 1d
```

#### Windows (Планировщик задач)

1. Откройте Планировщик задач
2. Создайте простую задачу
3. Установите триггер (Ежедневно, Еженедельно и т.д.)
4. Установите действие: `python src/app/main.py analyze --symbol EURUSD --timeframe 1h`
5. Установите рабочую директорию на корень проекта

#### Systemd Service (Linux)

Создайте `/etc/systemd/system/trading-assistant.service`:

```ini
[Unit]
Description=Trading Research Assistant
After=network.target

[Service]
Type=oneshot
User=your_user
WorkingDirectory=/path/to/project
ExecStart=/usr/bin/python3 src/app/main.py analyze --symbol EURUSD --timeframe 1h
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

Затем создайте таймер:

```ini
[Unit]
Description=Run Trading Assistant Hourly

[Timer]
OnCalendar=hourly
Persistent=true

[Install]
WantedBy=timers.target
```

</details>

## Советы и рекомендации

1. **Начните с тестирования**: Используйте демо-счет и небольшие суммы для проверки системы
2. **Ведите журнал**: Регулярно логируйте результаты для анализа эффективности
3. **Мониторьте уверенность**: Обращайте внимание на уровень уверенности в рекомендациях
4. **Используйте несколько таймфреймов**: Анализируйте один символ на разных таймфреймах
5. **Проверяйте новости**: Учитывайте фундаментальный анализ при принятии решений
6. **Регулярно обновляйте**: Следите за обновлениями системы и зависимостей

## Дополнительная информация

<details>
<summary><strong>📚 Связанная документация</strong></summary>

- [Архитектура проекта](./architecture.md) - Подробная документация по архитектуре
- [Правила импортов](./import_rules.md) - Правила зависимостей модулей
- [Политика безопасности](./safety_policy.md) - Политики управления рисками
- [Руководство по устранению неполадок](./troubleshooting.md) - Распространенные проблемы и решения
- [Обзор проекта](./overview.md) - Обзор проекта и возможности

</details>

---

<div align="center">

[📖 Обзор](./overview.md) • [🏗️ Архитектура](./architecture.md) • [🔧 Устранение неполадок](./troubleshooting.md) • [🔒 Политика безопасности](./safety_policy.md)

</div>